// Rust program to find age

use std::io;

fn main() {
	println!("\nStudent ages");
    let mut age = String::new();
    io::stdin().read_line(&mut age).expect("Failed to read input")

    // Ask user for age
    println!("Enter age of Students");
    let mut age = String::new();
    io::stdin().read_line(&mut age).expect("Failed to read input")
    let age:u32 = age.trim().parse.expect("Please enter valid number")
    if age >= 40 {}
}